---
name: Bug report
about: Report a problem with wpfy
title: ''
labels: bug
assignees: ''
---

<!--
⚠️ Before posting: REDACT anything sensitive.
Replace real domains with example.com, real IPs with 192.0.2.x,
and never paste passwords, .env contents, or backup archives.
Security vulnerabilities: do NOT report here — see SECURITY.md.
-->

**What happened?**
A clear description of the bug.

**What did you expect to happen?**

**Command(s) run**
```bash
# exact command(s), with domains redacted
```

**Output / error**
```text
# paste relevant output here
```

**Diagnostics**
```text
# output of: wpfy debug  (redact domains if you prefer)
```

**Environment**
- wpfy version (`wpfy --version`):
- OS (`lsb_release -ds` or `/etc/os-release`):
- Docker version (`docker --version`):
- Docker Compose version (`docker compose version`):
- VPS provider / virtualization (if relevant):
- Fresh server or existing workloads?

**Additional context**
Anything else that might help (DNS setup, Cloudflare, etc.).
